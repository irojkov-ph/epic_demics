function system = switch_cells(k,l,m,n,sys)
    system = sys;
    
    n_fields = fieldnames(system);
    for k=1:numel(n_fields)
        if( isnumeric(mystruct.(n_fields{k})) )
            
            system(k,l)=sys(m,n);
            system(m,n)=sys(k,l);
        end
    end

end