# Latex compilation files are ignored :
*.aux
*.fdb_latexmk
*.fls
*.log
*.synctex.gz
*.toc
	
# Temporary Matlab files are ignored :
*.asv
*.m~

# Ignore the `main.m` script because it vary with simulations
main.m

# Compressed files are ignored :
*.rar
*.zip

# Files begining with `my_` are ignored :
my_*

# The folder `./doc/papers/`
papers/

# The certain files in folder `./logs/`
logs/*.fig
logs/*.mat
logs/displacements.*
